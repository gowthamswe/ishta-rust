# Fixtures that FAIL the memory_sanitizer suite under KARAC_SANITIZE_ADDRESS=1
# at KARAC_OPT_LEVEL=0. Consumed by scripts/asan-instrumented-leg.sh.
# B-2026-09-07-40.
#
# Every entry names the bug row that owns it. Nothing goes on this list without
# one — an unattributed entry is indistinguishable from a fixture someone
# silenced.
#
# THE LIST ONLY SHRINKS. The leg fails both when an unlisted fixture starts
# failing (a regression) and when a listed one starts PASSING (the owning bug
# got fixed and the line is now stale). That second direction is what keeps this
# from decaying into a permanent allowlist.
#
# WHY THIS LIST IS SEPARATE FROM tests/asan-o0-known-failures.txt, rather than
# shared with it. This leg sees a strictly LARGER error class: the `-O0` leg is
# the allocator gate with real (unoptimized-away) allocations, this one adds
# ASAN's memory-ACCESS checking on top. Its failure set is therefore a superset,
# and merging the two would let an access-class regression hide behind an
# allocator-class quarantine entry that happens to name the same fixture.
#
# SEEDED 2026-09-07 on 4e4b9fe17 plus this row's instrumentation change (the
# local SHA is rewritten by the pre-push rebase, so the public base is cited
# instead), x86_64 Linux, both archives rebuilt lean-then-full,
# KARAC_SANITIZE_ADDRESS=1 KARAC_OPT_LEVEL=0, --test-threads=8:
# 1518 passed / 6 failed / 9 ignored in 354s.
#
# Two of the six are the `-O0` leg's existing quarantine, failing here for the
# same allocator-class reasons and owned by the same row. FOUR are new, and every
# one of them is an ACCESS-class defect that no gate in this repo could report
# before this leg existed — three heap use-after-free reads and one stack
# overread, in fixtures named `no_uaf`, `no_double_free` and `no_leak`. That
# ratio is the row's own argument: the first run of the access gate found four
# real defects the allocator gate had been green on.
#
# The list SHRANK once before it was ever committed, which is the ratchet working
# rather than an accident. It was first seeded at 733316f18 with SEVEN entries,
# the extra one being
# `asan_stored_argument_is_owned_by_its_new_home_not_the_caller` under
# B-2026-09-07-20. Rebasing onto `dffb1d179` — another session's fix, which
# closed that row and took the same line off the `-O0` list — made the fixture
# pass here too, and re-measuring rather than carrying the line forward is what
# kept this file from being born with a stale entry.

# ── Carried over from the -O0 leg (allocator class; same row owns them) ──
memory_sanitizer_tests::asan_discarded_branch_literal_field_over_a_loop_outer_local_declines   # B-2026-09-07-43
memory_sanitizer_tests::asan_discarded_literal_projected_field_if_else_read_after_declines     # B-2026-09-07-43

# ── New, and visible only under instrumentation (access class) ──
#
# B-2026-09-07-54 REMOVED 2026-09-08, its owning row fixed. The entry read:
#
#   `__karac_vec_elem_rc_dec_Node` reads offset 0 of a 32-byte block that
#   `__karac_rc_drop_Node` already freed. No double free, no leak.
#   memory_sanitizer_tests::asan_let_bound_vec_option_shared_reused_no_uaf
#
# The cause was an untyped `let s = <receiver>.clone()` yielding
# `Option[shared T]`, which no case in stmts.rs's untyped-let chain registered
# into the caller-retains model: each by-value pass decremented without a
# matching arg-site inc, so two passes drove the element's refcount to zero
# while the container still held it. This fixture reads `src[0].clone()` and is
# named for the BARE `let s = v[i]` spelling (case (f)) that the typechecker no
# longer accepts -- `E_INDEX_MOVE_NON_COPY`, whose own remedy text names
# `v[i].clone()`. So it had been testing the unregistered path under the
# registered path's name.
#
# The lesson worth keeping is about this file rather than that fix. "No double
# free, no leak" was a property of the FIXTURE'S TWO PASSES, not of the defect:
# a third pass drives the count to -1 and frees the block twice, which glibc
# reports as `malloc(): unaligned tcache chunk detected`. A quarantine entry
# records what one program did on one leg -- it is not a characterization of the
# bug, and reading it as one costs the escalation.
#
# `__karac_rc_drop_E` frees a 120-byte block and reads it again — both ends
# inside the same generated drop.
memory_sanitizer_tests::asan_shared_enum_boxed_struct_payload_moveout_no_double_free           # B-2026-09-07-55
#
# `v[i] = <place>` releases the old element, then `main` reads through the
# released 16-byte handle. Read is in `main`, not in a teardown helper.
memory_sanitizer_tests::asan_vec_plain_shared_index_overwrite_place_rhs_no_leak                # B-2026-09-07-56
#
# The one NON-heap finding: `stack-buffer-overflow`, a read at offset 40 of a
# frame that has exactly one object. Same fixture family as B-2026-09-07-43's
# two lines above, filed separately because a stack overread is a different
# failure mode from that row's leak.
memory_sanitizer_tests::asan_discarded_literal_projected_field_keeps_its_owner                 # B-2026-09-07-57
